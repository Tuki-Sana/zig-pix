.zig-cache/
zig-out/
dist/

# mise local override (グローバル設定があれば不要)
# .mise.toml

# Node.js
node_modules/
package-lock.json
js/dist/

# Pre-built native binaries (generated by CI, not committed to git)
npm/*/libpict.dylib
npm/*/libpict.so

# OS
.DS_Store
