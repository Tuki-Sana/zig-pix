.zig-cache/
zig-out/
dist/

# Emscripten / cmake intermediate build artifacts
build/

# mise local override (グローバル設定があれば不要)
# .mise.toml

# Node.js
node_modules/
package-lock.json
js/dist/

# Pre-built native binaries for npm optional packages (zigpix-darwin-arm64 / zigpix-linux-x64)。
# CI artifacts をここへコピーしてから `npm publish` する。リポジトリにはコミットしない。
npm/*/libpict.dylib
npm/*/libpict.so

# 個人・機密を含み得るローカルメモ（テンプレは docs/LOCAL.example.md）
docs/LOCAL.md

# Benchmark results (generated by CI/local runs, not committed)
bench/results/

# OS
.DS_Store
